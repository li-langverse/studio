# Archived studio demo assets

**Not for user-facing demo video.** These HTML marketing mocks were moved out of the default capture path so `record-studio-verticals-demo.sh` only encodes native frames.

| Path | Purpose |
|------|---------|
| `verticals-html-mocks/` | Per-vertical Chrome/HTML screenshots (internal layout reference only) |

User demo MP4: `docs/demo/media/studio-verticals-demo.mp4` (native CPU present host + `LIG_HOST_PRESENT=1`).
