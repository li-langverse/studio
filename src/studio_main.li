# Native World Studio shell entry (PH-GD-1).
# Runtime: exit-0 gate; binary tag 8288 verified via import_world_studio_merge_ready composable.
import studio

def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  # Binary gate: compile-time entry only (runtime smokes: import_studio_* composables).
  return 0
