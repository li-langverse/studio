# Sync studio from lic main @ 7c0c373

**Status:** Ready for review  
**Repo:** li-langverse/studio  
**PR:** (this branch)  
**PH / REQ:** PH-SIM vertical gaps, PH-AGENT MCP, PH-HW (`lig`)  
**Author:** agent

---

## Summary (one sentence)

Org **studio** package tree matches lic `packages/li-studio` at `7c0c37370259ddc2be0b9d720ac0dfb44c3a738e`, including World Studio vertical profile examples and `lig` dependency pins (replacing stale `li-gpu` naming).

## Agent continuation (required)

1. Read: `lic` `main` `packages/li-studio/` if drift suspected; re-run rsync from that path into this repo (exclude `docs/demo`, `scripts`, `.github`, `.cursor`).
2. Run: package smokes when `lic` + sibling `li-gui` / `li-ui` / `li-sim` / `lig` are on PATH or in monorepo layout.
3. Then: wire host `lig.present` in native shell (WP3); keep demo reel provenance honest (`native_window: false` until PH-GD-5).
4. Blocked on: full wgpu viewport CI in org CI until **lig** + **render** mirrors publish matching lic versions.

## Changed (specific)

| Area | What | Evidence |
|------|------|----------|
| Package source | `src/lib.li`, `src/main.li` — UX-11 invoke, sim-step hooks, MCP extended | `git diff 3984cbf..7c0c373 -- packages/li-studio` in lic |
| Vertical profiles | `examples/verticals/{game,sim_*}/studio.toml` + README | Seven dirs copied from lic |
| Manifest / smokes | +4 smokes; manifest entries for agent invoke, MCP extended, sim step, vertical demo env | `li-tests/manifest.toml`, `li-tests/smoke/*.li` |
| Pins | `li.toml` adds `lig`, scene/physics/sim vertical deps | No `li-gpu` path dep |
| README | `import lig` / present bridge docs | PH-HW |

## Not changed (scope fence)

- `docs/demo/**`, `scripts/record-studio-x-demo.sh`, `.github/**` — org-only marketing/demo assets.
- **ui** / **render** org repos — separate packages; ui already at lic `3984cbf` parity (no `li-ui` diff since then).
- **lic** compiler / trusted.lean — not touched here.
- Native SDL/wgpu host window capture — still blocked on PH-GD-5.

## Breaking changes

None — `import studio` surface unchanged; `studio_profile_sim_robotics()` id `4` already landed in prior lic sync.

## Security

N/A — IR-only compose/paint; no new trusted axioms or FFI in this sync.

## Performance

N/A — no bench threshold changes in org mirror; viewport perf remains lic `scripts/bench-studio-viewport-perf.sh`.

## Downstream

| Repo | Action |
|------|--------|
| ui | Already synced @ `3984cbf`; no PR required for this lic slice |
| render / lig | Consumers pin after lic publishes; studio `li.toml` documents monorepo paths only |

## CHANGELOG entry (paste into Unreleased)

Synced from lic `main` @ `7c0c373` — vertical examples, sim-step/MCP smokes, `lig` deps.
